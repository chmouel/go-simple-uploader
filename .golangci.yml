version: 2

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - gocritic
    - gosec
    - misspell
    - unconvert
    - goconst
    - testifylint
    - usestdlibvars
  settings:
    errcheck:
      exclude-functions:
        - flag.Set
        - logger.Sync
        - fmt.Fprintf
        - fmt.Fprintln
        - os.Stdout
        - (io.Closer).Close
    revive:
      rules:
        - name: exported
          severity: warning
    goconst:
      find-duplicates: true
      min-len: 10
      min-occurrences: 10
      ignore-tests: true
    gocritic:
      enabled-tags:
        - diagnostic
        - experimental
        - opinionated
        - performance
        - style
      disabled-checks:
        - whyNoLint
        - wrapperFunc
        - hugeParam
        - unnamedResult
        - evalOrder
        - appendCombine
        - nestingReduce
    gosec:
      excludes:
        - G122  # Filesystem operation in filepath.Walk/WalkDir callback
        - G306  # Expect WriteFile permissions to be less permissive
        - G703  # Path traversal via taint analysis (safe when paths are from known roots)
